{
  "type": "string",
  "description": "n8n expression (starts with = or {{ }})",
  "pattern": "^(=.*|\\{\\{.*\\}\\})$",
  "examples": [
    "={{ $json.fieldName }}",
    "={{ $node[\"NodeName\"].json.field }}",
    "={{ $now.format(\"yyyy-MM-dd\") }}"
  ]
}