{
  "type": "object",
  "description": "Reference to a stored credential",
  "properties": {
    "id": {
      "type": "string",
      "description": "Credential ID"
    },
    "name": {
      "type": "string",
      "description": "Credential name"
    }
  }
}